name: Build, Push, and Update GitOps

on:
  push:
    branches: [ "main", "staging", "dev" ]

concurrency:
  group: gitops-${{ github.ref }}
  cancel-in-progress: false

env:
  IMAGE_REPO: ghcr.io/votingm7011e/hello-world
  GITOPS_REPO: votingm7011e/gitops
  GITOPS_BRANCH: main
  APP_FILE: hello-world.yaml

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Detect changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            build:
              - 'Dockerfile'
              - 'app.py'
              - '.github/workflows/**'

      - name: Stop early if no build needed
        if: steps.filter.outputs.build != 'true'
        run: echo "No relevant changes; skipping." && exit 0

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_REPO }}
          tags: |
            type=raw,value=${{ github.sha }}

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Resolve target environment
        id: resolve
        shell: bash
        run: |
          case "${GITHUB_REF_NAME}" in
            main)    TARGET_ENV="dev" ;;
            staging) TARGET_ENV="staging" ;;
            dev)     TARGET_ENV="dev" ;;
            *) echo "Unsupported branch '${GITHUB_REF_NAME}'. "; exit 1 ;;
          esac
          echo "TARGET_ENV=$TARGET_ENV" >> "$GITHUB_OUTPUT"

      - name: Clone GitOps repo
        run: |
          git clone --depth 1 --branch "${{ env.GITOPS_BRANCH }}" "https://x-access-token:${{ secrets.GITOPS_TOKEN }}@github.com/${{ env.GITOPS_REPO }}.git" gitops-repo

      - name: Get current image tag from GitOps values
        id: current_tag
        uses: mikefarah/yq@master
        with:
          cmd: yq '.image.tag' "gitops-repo/environments/${{ steps.resolve.outputs.TARGET_ENV }}/${{ env.APP_FILE }}"

      - name: Compare and update GitOps values
        id: diff
        env:
          IMAGE_TAG: ${{ steps.meta.outputs.tag-names }}
          CURRENT_TAG: ${{ steps.current_tag.outputs.result }}
        run: |
          cd gitops-repo
          NEW_TAG=$(echo "$IMAGE_TAG" | cut -d',' -f1)
          echo "Current tag: $CURRENT_TAG"
          echo "New tag    : $IMAGE_TAG"
          if [[ "$CURRENT_TAG" != "$IMAGE_TAG" ]]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push GitOps update
        if: steps.diff.outputs.changed == 'true'
        env:
          IMAGE_TAG: ${{ steps.meta.outputs.tag-names }}
        run: |
          cd gitops-repo
          git config user.name "GitOps CI"
          git config user.email "actions@github.com"
          git add .
          git commit -m "chore: update image tag to ${{ env.IMAGE_TAG }} [skip ci]"
          git push origin "${{ env.GITOPS_BRANCH }}"